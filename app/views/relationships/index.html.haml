
%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |relationship|
        %tr
          %td
            %img
              = gravatar_for(relationship.leader)
            = link_to relationship.leader.full_name, user_path(relationship.leader)
          %td.extra-padding
            = relationship.leader.queue_items.count
          %td.extra-padding
            = # relationship.leader.leader_relationships.count
          %td.extra-padding
            = link_to relationships_path, method: :delete do
              %i.glyphicon.glyphicon-remove
